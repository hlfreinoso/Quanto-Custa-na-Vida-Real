{
  "name": "quanto-custa-na-vida-real",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo do app Quanto custa na vida real.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "echo \"No lint configured yet\"",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p packages/core/tsconfig.json && tsc -p packages/ui/tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
