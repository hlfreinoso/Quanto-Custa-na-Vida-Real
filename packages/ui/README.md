# UI

Pacote reservado para componentes e tokens compartilhaveis.

No MVP, componentes podem nascer dentro de `apps/mobile`. Este pacote deve ser usado apenas quando houver ganho real de reutilizacao.
